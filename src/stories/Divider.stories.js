import Divider from "../components/DataDisplay/Divider";

export default {
  title: "DataDisplay/Divider",
  component: Divider,
};

const Template = (args) => <Divider {...args} />;

export const MuiDivider = Template.bind({});

MuiDivider.args = {};
